# This will be sourced as a bash script

# Tormod Volden 2008-05-25
# Debian source package name is old and awkward, ugly hack:

CURDIR=`pwd`
OLDNAME=`basename ${CURDIR}`
cd ..
NEWNAME=`echo $OLDNAME | sed 's/xserver-xorg-input-/xfree86-driver-/'`
mv $OLDNAME $NEWNAME
cd $NEWNAME
DEBPKG="xfree86-driver-synaptics"

CHANGES+=("(Renamed source package to xfree86-driver-synaptics)")

