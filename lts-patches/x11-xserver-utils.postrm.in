#!/bin/sh -e
set -e

if [ remove = "$1" -o abort-install = "$1" -o disappear = "$1" ]; then
    dpkg-divert --remove --package x11-xserver-utils-@SUFFIX@ --rename \
        --divert /usr/bin/xrandr.orig /usr/bin/xrandr
fi

#DEBHELPER#

exit 0
