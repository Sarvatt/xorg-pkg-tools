# This will be sourced as a bash script

# Tormod Volden 2009-01-02
# 

sed -i '/^Architecture: /s/$/ lpia/' debian/control
CHANGES+=("Added lpia architecture")

sed -i '/^ x11proto-xf86dri-dev,/s/$/ x11proto-dri2-dev,/' debian/control
CHANGES+=("Add build-dep on x11proto-dri2-dev")

